# Human Annotation Workflow

## Purpose

The human annotation workflow converts stored model responses into
auditable criterion-level annotations and deterministic response scores.

The workflow is deliberately separated into two stages:

```text
human judgment
      ↓
deterministic scoring